@model web.Models.ViewAddress

@{
	ViewBag.Title = "Add apartment/office number";
	Layout = "~/Views/Shared/_LayoutModeling.cshtml";
}

<h2>Add apartment/office number</h2>

<p>
	@Html.ActionLink("Locations", "Index") |
	@Html.ActionLink("Change", "Select")
</p>

@using (Html.BeginForm())
{
	@Html.ValidationSummary(true)
	@Html.Hidden("returnTo", (String)ViewBag.ReturnTo)
	@Html.Hidden("countryID", (Int64)ViewBag.CountryID)
	@Html.Hidden("stateID", (Int64)ViewBag.StateID)
	@Html.Hidden("cityID", (Int64)ViewBag.CityID)
	@Html.Hidden("streetID", (Int64)ViewBag.StreetID)
	@Html.Hidden("buildingID", (Int64)ViewBag.BuildingID)
	<fieldset>
		<legend></legend>

		<div class="editor-label">
			@Html.LabelFor(model => model.Apartament, "Apartment/Office")
		</div>
		<div class="editor-field">
			@Html.EditorFor(model => model.Apartament)
			@Html.ValidationMessageFor(model => model.Apartament)
		</div>
	</fieldset>
<p>
	<input type="submit" value="Add Value" />
</p>
}
